# Основные зависимости для Telegram-бота поиска юзернеймов

# Aiogram 3.x - фреймворк для Telegram ботов
aiogram==3.7.0

# Асинхронная работа с SQLite
aiosqlite==0.20.0

# Загрузка переменных окружения из .env файла
python-dotenv==1.0.1

# HTTP клиент для проверки Fragment (уже включен в aiogram, но указываем явно)
aiohttp>=3.9.0

# Fragment.com API (точная проверка статуса юзернеймов)
python-fragment>=0.2.2

# SOCKS5 прокси поддержка
aiohttp-socks>=0.8.0

# Дополнительные зависимости (устанавливаются автоматически)
# pydantic - для валидации данных (зависимость aiogram)
